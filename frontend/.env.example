# Base URL of the deployed backend API.
# Local dev defaults to http://localhost:8000 when this is unset.
# On Vercel, set this to your Render backend URL (no trailing slash):
# VITE_API_URL=https://order-management-api.onrender.com
VITE_API_URL=http://localhost:8000
